strictness: veryhigh
test-warnings: true
doc-warnings: false

ignore-paths:
  - docs

ignore-patterns:
  - __init__.py
  - conftest.py

pep8:
  options:
    max-line-length: 120
pylint:
  options:
    max-line-length: 120
